---
const { title } = Astro.props;
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title} | LocalChains.org</title>
    <style is:global>
      /* The "Joshua Kaplan" Brutalist Reset */
      body {
        font-family: "Courier New", Courier, monospace;
        margin: 0;
        padding: 3rem 2rem;
        max-width: 900px;
        margin-left: auto; 
        margin-right: auto;
        background-color: white;
        color: black;
      }
      a { color: black; text-decoration: none; border-bottom: 1px solid #ccc; }
      a:hover { background-color: black; color: white; border-bottom: none; }
      h1 { font-size: 3rem; letter-spacing: -1px; margin-bottom: 0.5rem; line-height: 1; }
      h2 { font-size: 1.5rem; margin-top: 2rem; text-transform: uppercase; letter-spacing: 0.5px; }
      p { font-size: 1.1rem; line-height: 1.5; color: #333; }
      ul { list-style: none; padding: 0; }
    </style>
  </head>
  <body>
    <nav style="margin-bottom: 4rem;">
      <a href={import.meta.env.BASE_URL} style="font-weight: bold; border: none;">LocalChains.org</a>
    </nav>
    
    <slot />
  </body>
</html>